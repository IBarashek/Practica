<Page x:Class="Practica.pages.NavigationPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Practica.pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="NavigationPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="4*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Button x:Name="BtnAdd" Content="Добавить" Click="ClickNavigation"/>
            <Button x:Name="BtnEdit" Content="Изменить" Click="ClickNavigation"/>
            <Button x:Name="BtnDelete" Content="Удалить" Click="ClickNavigation"/>
            <Button x:Name="BtnFilter" Content="Группы" Click="ClickNavigation"/>
            <Button x:Name="BtnShow" Content="Все студенты" Click="ClickNavigation"/>
        </StackPanel>
        <Frame Grid.Row="1" x:Name="NavigationFrame"/>
    </Grid>
</Page>
